const resultado = nota => nota >= 7 ? 'aprovado' : 'reprovado' // parametro nota
console.log(resultado(7.1))
console.log(resultado(6.1))

/*const resultado = nota => {
    return nota >= 7 ? 'aprovado' : 'reprovado'
}
com chaves */

let media = 5.5

media > 7 ? 'aprovado' : 'reprovado'


