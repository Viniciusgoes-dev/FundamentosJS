const mostrar_resultado = function(nota) { // jogando a função dentro da variavel
    if(nota >= 6) {
        console.log('Aprovado!')
    } else {
        console.log('Reprovado!')
    }
}

mostrar_resultado(9)
mostrar_resultado(5)